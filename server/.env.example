# Server Configuration
PORT=3001
NODE_ENV=development

# Frontend URL
FRONTEND_URL=http://localhost:5173

# OAuth Configuration - Replace with your actual credentials
# Google OAuth (YouTube API) - Get from https://console.cloud.google.com/
# 1. Create a new project or select existing project
# 2. Enable YouTube Data API v3
# 3. Create OAuth 2.0 Client ID credentials
# 4. Add your redirect URI: http://localhost:3001/auth/youtube/callback
GOOGLE_CLIENT_ID=your_actual_google_client_id_here
GOOGLE_CLIENT_SECRET=your_actual_google_client_secret_here

# Facebook OAuth (Facebook/Instagram API) - Get from https://developers.facebook.com/
# 1. Create a new app or select existing app
# 2. Add Facebook Login product
# 3. Set redirect URI: http://localhost:3001/auth/facebook/callback
# 4. Add required permissions: email, public_profile, pages_show_list, etc.
FACEBOOK_APP_ID=your_actual_facebook_app_id_here
FACEBOOK_APP_SECRET=your_actual_facebook_app_secret_here

# Session Secret - Generate a secure random string (minimum 32 characters)
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your_secure_session_secret_min_32_characters_change_this